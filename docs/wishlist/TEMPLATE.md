# <Feature name>

One feature per file. A wish is a *seed* — what you want and why — not a design.
Keep it light; designing it properly happens later, when it graduates into a
[spec](../spec/spec.md) section and a [plan](../plans/index.md).

| | |
|--|--|
| **Added** | YYYY-MM-DD |
| **Status** | `idea` \| `accepted` \| `designing` \| `done` \| `dropped` |
| **Touches** | rough guess at affected areas — `product` / `engineering` / `design`, or specific repos/views (or `?`) |

## What

The feature in a sentence or two, from the user's point of view. What can they
do that they can't today?

## Why

The pain it removes or the value it adds. What makes it worth doing.

## Notes (optional)

Rough thoughts, sketches, open questions, links, prior art. Anything that would
otherwise get lost. No need to resolve them here.

---

<!-- When this graduates, link out and set Status:
- Designed in: spec/<section> · plan: plans/<file>
This line is the bridge from "wanted" to "being built / shipped". -->
